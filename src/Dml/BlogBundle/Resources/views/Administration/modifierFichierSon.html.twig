{% extends 'DmlBlogBundle::layout_admin.html.twig' %}

{% block title %}{{ parent() }} - Modifier fichier son{% endblock %}

{% block titre_admin %} - Fichier son {% endblock %}

{% block body %}
	<div class="row">
		<div class="col-md-12">
			<form action="{{ path('dmlblog_modifier_fichier_son', {'fichierSon': fichierSon.id}) }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" role="form">
				<fieldset>
					<legend>Modifier un fichier son</legend>
					{{ form_errors(form) }}
					<div class="form-group">
						{{ form_label(form.nom) }}
						<div class="col-md-3">
							{{ form_widget(form.nom) }}
						</div>
					</div>
					<div class="form-group">
						{{ form_label(form.fichier) }}
						<div class="col-md-3">
							{{ form_widget(form.fichier) }}
						</div>
					</div>
					{{ form_rest(form) }}
					<div class="form-group">
						<div class="col-md-offset-2 col-md-10">
							<a href="{{ path('dmlblog_modifier_supprimer_utilisateur') }}" class="btn btn-info"><span class="glyphicon glyphicon-arrow-left"></span> Retour</a>
							<button type="submit" class="btn btn-primary">Valider <span class="glyphicon glyphicon-ok"></span></button>
						</div>
					</div>
				</fieldset>

			</form>
		</div>
	</div>
{% endblock %}
